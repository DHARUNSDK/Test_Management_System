// Automated Grading Logic
function gradeMCQ(answer, correctAnswer) {
    return answer === correctAnswer ? "Correct" : "Incorrect";
  }
  
  // Manual Grading Placeholder
  function gradeSubjective(response) {
    console.log(`Instructor needs to grade: ${response}`);
  }
  